# Седмица 9
# Предефиниране на оператори - по-дълбоки теми. Специални оператори. Използване. Още шаблони за проектиране.



<div align="justify">



## Задача 1 🧩 (Комплексни Числа) 🧩

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/easy-green)

### 📖 Описание:

Създайте клас `ComplexNumber`, представляващ комплексно число. Класът притежава следния интерфейс:

* Подразбиращ се конструктор `ComplexNumber()`;
* Параметризиран конструктор `ComplexNumber(double, double)`;
* Селектори за всички полета - реалната и имагинерната части.

Класът притежава следните предефинирани оператори:

* Оператор `+` за събиране на две комплексни числа;
* Оператор `-` за изваждане на две комплексни числа;
* Оператор `*` за умножение на две комплексни числа;
* Оператор `/` за деление на две комплексни числа;
* Оператор `-` за получаване на комплексно спрегнато число;
* Оператор `>>` за работа на комплексно число върху входен поток;
* Оператор `<<` за работа на комплексно число върху изходен поток.

Върху комплексните числа дефинираме наредба, която съответства на конюнкция от идентичната наредба, приложена покомпонентно върху комплексните числа. Предефинирайте следните оператори за класа:

* Оператор `<` за строга частична наредба;
* Оператор `>` за строга частична наредба;
* Оператор `<=` за нестрога частична наредба;
* Оператор `>=` за нестрога частична наредба;
* Оператор `==` между две комплексни числа;
* Оператор `!=` между две комплексни числа.

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 2 🔀 (Вектори) 🔀

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/easy-green)

### 📖 Описание:

Създайте клас `Vector`, представляващ математически вектор. Класът притежава следния интерфейс:

* Подразбиращ се конструктор `Vector()`;
* Параметризиран конструктор `Vector(double, double, double)`;
* Селектори за всички полета - трите координати.

Класът притежава следните предефинирани оператори:

* Оператор `+` за събиране на вектори.
* Оператор `-` за изваждане на вектори.
* Оператор `*` за умножение на вектор със скалар.
* Оператор `*` за умножение на вектор с вектор.
* Оператор за преобразуване към `bool`, показващ дали два вектора са успоредни.
* Оператор `()` за взимане на дължината на вектор.
* Оператор `[]` за достъп до произволен компонент на вектор.
* Оператор `>>` за работа на математически вектор върху входен поток.
* Оператор `<<` за работа на математически вектор върху изходен поток.

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 3 𝄜 (Матрици) 𝄜

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/easy-green)

### 📖 Описание:

Създайте клас `Matrix`, който представлява матрица от цели числа. Класът трябва да има следният интерфейс:

* Подразбиращ се конструктор `Matrix()`, създаващ матрица с размери `1` и `1`;
* Параметризиран конструктор `Matrix(unsigned int, unsigned int)`, които да определят размера на матрицата.

Предефинирайте следните оператори за класа:

* Оператор `+` за събиране на матрици.
* Оператор `-` за изваждане на матрици.
* Оператор `*` за умножение на матрици.
* Оператор `*` за умножение на матрица със скалар.

* Оператор `++` за инкрементиране на елементите на матрицата.
* Оператор `--` за декрементиране на елементите на матрицата.

* Оператор `==`, показващ дали две матрици са с еднакви елементи.
* Оператор `!=`, показващ дали две матрици са с различни елементи.

* Оператор `[]`, връщаш ред на матрицата по зададен индекс.

* Оператор `<`, който проверява за съответната наредба над елементите на две матрици.
* Оператор `>`, който проверява за съответната наредба над елементите на две матрици.
* Оператор `<=`, който проверява за съответната наредба над елементите на две матрици.
* Оператор `>=`, който проверява за съответната наредба над елементите на две матрици.

* Оператор `()`, който транспонира матрицата.

* Оператор за преобразуване към `bool`, който връща резултат дали матрицата е нулевата.

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 4 ()

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/easy-green)

### 📖 Описание:

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 5 ⛌ (Полиноми) ⛌

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/medium-yellow)

### 📖 Описание:

Създайте клас `Polynomial`, представляващ полином на една променлива. Полиномът на една променлива се описва със степен и поредица от коефициенти. Класът притежава следния интерфейс:

* Подразбиращ се конструктор `Polynomial()`;
* Параметризиран конструктор `Polynomial(unsigned int, const double*)`;
* Копиращ конструктор `Polynomial(const Polynomial&)`;
* Деструктор `~Polynomial()`;
* Оператор за присвояване `Polynomial& operator=(const Polynomial&)`;

Предефинирайте следните оператори за класа:

* Оператор `+` за събиране на два полинома;
* Оператор `-` за изваждане на два полинома;
* Оператор `*` за умножение на два полинома;
* Оператор `+=` за събиране на два полинома;
* Оператор `-=` за изваждане на два полинома;
* Оператор `*=` за умножение на два полинома;
* Оператор `-`, който връща противоположния полином;
* Оператор `~`, който връща производната на полинома по променливата;
* Оператор `!`, който връща интеграла на полинома по променливата;
* Оператор `()`, който пресмята стойността на полинома в дадена стойност;
* Оператор `[]`, който връща коефициента на полинома пред някоя степен - константна версия;
* Оператор `[]`, който връща коефициента на полинома пред някоя степен - неконстантна версия;
* Оператор `==`, който проверява дали два полинома са еднакви;
* Оператор `!=`, който проверява дали два полинома не са еднакви.

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 6 ()

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/medium-yellow)

### 📖 Описание:

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 7 ()

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/medium-yellow)

### 📖 Описание:

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 8 ()

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/medium-yellow)

### 📖 Описание:

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 9 😩 (Мултимножества) 😩

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/hard-red)

### 📖 Описание:

Създайте клас `MultiSet`, представляващ мултимножество. Мултимножеството се описва с крайна граница `N`, което е цяло число, показващо, че числата в мултимножеството ще са в диапазона от `0` до `N`, и `K`, което е число, показващо колко най-много бита са необходими за запазването на броя срещания на дадено число, тук $(k \in [1; 8])$. Това означава, че всяко число може да се среща в множеството най-много `$(2^{k} - 1)$` пъти. Класът притежава следния интерфейс:

* Параметризиран конструктор `MultiSet(unsigned int, unsigned int)`;
* Копиращ конструктор `MultiSet(const MultiSet)`;
* Деструктор `~MultiSet()`;
* Оператор за присвояване `MultiSet& operator=(const MultiSet&)`;
* Метод за добавяне на число към мултимножеството;
* Метод за премахване на число от мултимножеството;
* Метод за намиране на броя срещания на дадено число;
* Метод за извеждане на броя числа в мултимножеството;

Класът да има и следните оператори:

* Оператор `+`, който добавя число към мултимножеството;
* Оператор `-`, който премахва число от мултимножеството;
* Оператор `+=`, който добавя число към мултимножеството;
* Оператор `-=`, който премахва число от мултимножеството;
* Оператор `|`, който осъществява дизюнкция на две мултимножества;
* Оператор `&`, който осъществява конюнкция на две мултимножества;
* Оператор `^`, който осъществява разлика на две множества;
* Оператор `>>`, който осъществява работата с входен поток;
* Оператор `<<`, който осъществява работата с изходен поток.

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 10 ()

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/hard-red)

### 📖 Описание:

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 11 ()

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/hard-red)

### 📖 Описание:

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 12 ()

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/hard-red)

### 📖 Описание:

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 13 ()

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/impossible-black)

### 📖 Описание:

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:



</div>
