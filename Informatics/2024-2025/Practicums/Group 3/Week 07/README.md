# Седмица 7
# Още за класовете - влагане. Статични и константни членове на клас. Приятели. Контрол над създаването на обекти. Шаблони за проектиране. Примери.



# Задача 1


# Задача 2



# Задача 3

Създайте програма, която да съдържа клас `Computer`, който да съдържа следните член-данни:

* `serial number` - полето се генерира автоматично с последователни числа за всеки нов обект от класа.
* `brand` - символен низ с произволна дължина
* `processor` - символен низ с произволна дължина
* `video` - цяло число
* `hard drive` - цяло число
* `weight` - цяло число
* `battery life` - цяло число
* `price` - дробно число
* `quantity` - цяло число

Спазвайте принципа за енкапсулация на данните. Създайте конструктор, който приема всички полета. Следвайте принципа `Rule of 3`. Създайте селектори и мутатори за всички полета. Създайте функции за сериализация и десереализация в двоичен файл. Обработете всякакви проблеми с паметта, невалидни данни и други некоректни ситуации в жизнения цикъл на обектите, с подходящи изключения. Обектите от тип `Computer` трябва да са непроменими след създаването си.

Създайте клас `ComputerShop`, описващ компютърен магазин със следните характеристики:

* име - символен низ, с произволна дължина
* списък с компютри - приемаме, че няма да съдържа повече от `100` елемента.

Създайте параметризиран конструктор за класа и следните методи:

* добавяне на компютър - ако вече има компютър от съответната марка, увеличава количеството, ако няма, го добавя в списъка. Ако в списъка няма място, да се хвърли изключение.
* принтиране на списъка с компютри - извежда информация към подаден изходен поток.
* покупка на компютър - приема `brand` и пари на купувача. Ако купувачът има достатъчно пари спрямо цената и има достатъчно количество от съответния компютър, осъществява успешна покупка и намалява количеството. В противен сручай се хвърля подходящо изключение.
* метод, връщащ всички компютри с наличност, по-голяма от 0 - с опция да филтрира компютри, подходящи за пътуване или такива за игри.

Създайте клас, съдържащ само статични методи, `ConsultantUtils` с два метода:

* `isGoodForGaming()` - приема обект от клас `Computer` и връща `true`, ако процесорът е `i5` или `i7`, видео паметта е поне `2 GB`, и твърдият диск е поне `512 GB`.
* `isGoodForTravel()` - приема обект от клас `Computer` и връща `true`, ако теглото е не повече от `2.5` килограма и животът на батерията е над 6 часа.

Приемете, че във вашия свят има само един единствен магазин за компютри.


# Задача 4


# Задача 5

Създайте програма, която да съдържа клас, представляващ интервал `[ a , b ]` от реалната права, като този интервал ще бъде дискретен. Реализирайте следния интерфейс на класа:

* Подразбиращ се констуктор, който създава празен интервал.
* Параметризиран конструктор с границите на интервала.
* Метод, който връща дължината на интервала.
* Метод, който приема число и проверява дали е част от интервала.
* Метод, който връща броя на простите числа в интервала.
* Метод, който връща броя на числата палиндроми в интеравала.
* Метод, който връща броя на числата, които намят повтарящи се цифри.
* Метод, който проверява дали границите на интеравала са степени на числото `2`.
* Метод, който приема друг интервал, и връща сечението на текущия интервал с подадения.
* Метод, който приема друг интервал, и връща обединението на текущия интервал с подадения.
* Метод, който приема друг интервал, и връща резултат дали подадения интервал е подинтервал на текущия.
* Метод, който приема друг интервал, и връща резултат дали подадения интервал е надинтервал на текущия.



# Задача 6

Създайте програма, която да съдържа клас `Game`, който съдържа следните член-данни: заглавие, цена, достъпност. Заглавието е символен низ, заделен с точна големина. Добавете необходимите селектори и мутатори за класа, метод, който връща информация дали играта е безплатна, както и метод, който извежда информация за играта на стандартния изход.

Създайте клас `GamePlatform`, който съдържа поредица от игри. Реализирайте методи за добавяне на игра, за извеждане на игра по даден индекс, за извеждане на списък с всички игри в платформата, за извеждане на най-евтината игра, за извеждане на най-скъпата игра, за извеждане на всички безплатни игри, за премахване на игра от платформата. Добавете възможност за запазване и извличане на данните от платформата в текстов и в двоичен файл.


# Задача 7


# Задача 8

Създайте програма, която да съдържа клас за фирма. Фирмата да има работници. Работникът се характеризира чрез уникален идентификатор, име, фамилия, работна заплата, шеф и фирма, за която работи. Шефът е работник, който се разпознава по това, че е шеф сам на себе си. Една фирма се характеризир с име, шеф и списък от работници.


# Задача 9

Създайте програма, която да имплементира поведението на програмата `Hex Viewer`. При стартиране на програмата трябва да се въведе път до двоичен файл и да се зареди съдържанието на файла в паметта. След като файлът се е заредил в паметта, трябва да поддържате следните операции:

* Преглед на файла (`view`), с която команда се извеждат на стандартния изход байтовете на файла в шестнадесетична бройна система. След това да се извеждат на стандартния изход интерпретацията на байтовете като символи. Ако байтът отговаря на латинска буква, то да се извежда символа, в противен случай да се извежда символа точка.
* Промяна на байт по индекс (`change`).
* Премахване на последния байт във файла (`remove`).
* Добавяне на байт в края (`add`).
* Запазване на промените в същия файл (`save`).
* Запазване на промените в друг файл (`save as`).

Да се реализира прост интерфейс, с който да се извикват написаните функции през конзолата. 

Добавете проста система за регистриране на действия, която при затваряне на приложението извежда статистика за приложените операции като брой.


# Задача 10

Да се разработи система за управление на книги в библиотека.

Създайте клас `Book`, който описва книга в библиотеката. Всяка книга има уникален идентификатор, който се генерира автоматично, заглавие - символен низ с произволна дължина, автор - символен низ с произволна дължина, статус - символен низ с произволна дължина.

Създайте клас `User` - описва потребител, който може да заема книги. Всеки потребител има потребителско име до 8 символа, телефонен номер за връзка до 10 символа.

Създайте клас `Library` - библиотека, която съдържа списък от книги. Реализирайте следните функционалности:

* Добавяне на нова книга, ако капацитетът го позволява.
* Заемане на книга по даден идентификатор и име на потребителя - отбелязва книгата като "заета" и кой потребител я е заел.
* Връщане на книга по даден идентификатор - отбелязва книгата като "налична".
* Извеждане на информация на стандартния изход за идентификатора на книгата, заглавието, автора и статуса.

Помислете за възможните ситуации, които биха могли да възникнат при работата на програмата и ги обработете с подходящи изключения.


# Задача 11

Създайте клас `Person`, който се характеризира с първо име, трето име, години, и два указателя към инстанции на класа - съответно майката и бащата на индивида. Създайте и клас `Family`, който съдържа двама души - майка и баща и указатели към три деца. В едно семейство майката и бащата да не могат да се променят. Направете съответните методи за извеждане на майката в едно семейство, за извеждане на бащата в едно семейство, за извеждане на информацията за първото дете, за второто дете, за третото дете, за извеждане на информация за бабите и дядовците по майчина и бащина линия на децата. Направете и метод за добавяне на дете в семейството.


# Задача 12


# Задача 13
