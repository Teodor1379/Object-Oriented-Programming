# Седмица 6
# Класове и обекти - жизнен цикъл. Конструктори и деструктори. Инициализация. Изключения.



<div align="justify">



## Задача 1 🕷️ (Паяци) 🕷️

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/easy-green)

### 📖 Описание:

По света има много паяци. В нашия свят всеки паяк си има име и възраст. Ще ни се наложи да създадем система за тяхното местожителство.

Създайте клас `Spider`, представляващ паяк. Паякът се характеризира с име, който е символен низ, заделен с точна големина, и възраст, която е цяло положително число. Класът притежава следния интерфейс:

* Подразбиращ се конструктор `Spider()`;
* Параметризиран конструктор `Spider(const char*, unsigned int)`;
* Копиращ конструктор `Spider(const Spider&)`;
* Деструктор `~Spider()`;
* Оператор за присвояване `Spider& operator=(const Spider&)`;
* Селектори за всички полета - полетата за име и възраст;
* Метод за извеждане на информацията на стандартния изход.

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, незамисимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 2 🍫 (Шоколади) 🍫

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/easy-green)

### 📖 Описание:

Тео много обича шоколад. Затова създайте програма, в която той да може да записва своите шоколади.

Създайте клас `Chocolate`, представляващ шоколад. Шоколадът се характеризира с марка, която е символен низ, заделен с точна големина, цена, която е дробно число, и грамаж, който е дробно число. Класът притежава следния интерфейс:

* Подразбиращ се конструктор `Chocolate()`;
* Параметризиран конструктор `Chocolate(const char*, double, double)`;
* Копиращ конструктор `Chocolate(const Chocolate&)`;
* Деструктор `~Chocolate()`;
* Оператор за присвояване `Chocolate& operator=(const Chocolate&)`;
* Селектори за всички полета - полетата за марка, цена и грамаж;
* Мутатори за всички полета - полетата за марка, цена и грамаж;
* Метод за извеждане на информацията на стандартния изход.

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 3 👨 (Хора) 👩

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/easy-green)

### 📖 Описание:

Системите за записи на хора обикновено са изключително важни за добрата функционалност на един технологичен свят.

Създайте клас `Person`, представляващ човек. Човекът се характеризира с първо име, което е символен низ, заделен с точна големина, трето име, което е символен низ, заделен с точна големина, възраст, която е цяло положително число, и пол, който е изброима стойност. Класът притежава следния интерфейс:

* Подразбиращ се конструктор `Person()`;
* Параметризиран конструктор `Person(const char*, const char*, unsigned int, const Sex&)`;
* Копиращ конструктор `Person(const Person&)`;
* Деструктор `~Person()`;
* Оператор за присвояване `Person& operator=(const Person&)`;
* Селектори за всички полета - полетата за първо име, трето име, възраст и пол;
* Мутатори за всички полета - полетата за първо име, трето име, възраст и пол;
* Метод за извеждане на информацията на стандартния изход.

Създайте и функции, които приемат трима души и определят кой е най-младият и кой е най-старият.

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 4 🎵 (Песни) 🎵

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/easy-green)

### 📖 Описание:

Джон по цял ден слуша музика и иска да създаде система, в която да пази своите песни, но не разбира почти нищичко от ООП.

Създайте клас `Song`, представляващ песен. Песента се характеризира с име на песента, име на изпълнител, които са символни низове, заделени с точна големина, и продължителност, която е цяло положително число. Класът притежава следния интерфейс:

* Подразбиращ се конструктор `Song()`;
* Параметризиран конструктор `Song(const char*, const char*, unsigned int)`;
* Копиращ конструктор `Song(const Song&)`;
* Деструктор `~Song()`;
* Оператор за присвояване `Song& operator=(const Song&)`;
* Селектори за всички полета - полетата за име на песен, име на изпълнител и продължителност;
* Мутатори за всички полета - полетата за име на песен, име на изпълнител и продължителност;
* Метод за извеждане на информацията на стандартния изход.

Създайте и функции, които намират онази песен с най-малка продължителност, онази песен с най-голяма продължителност, общата продължителност на песните, в масив от песни. Масивът е с максимална големина `13`. Демонстрирайте работата на вашата програма и работата на всички създадени функции. При некоректни данни да се хвърля изключение.

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 5 ⚡ (Динамичен Масив) ⚡

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/medium-yellow)

### 📖 Описание:

Създайте клас `Vector`, представляващ динамичен масив от цели числа, който ще позволява произволен размер на съдържанието си. Класът притежава следния интерфейс:

* Подразбиращ се конструктор `Vector()`;
* Параметризиран конструктор `Vector(unsigned int)` - версия с размер `size`;
* Параметризиран конструктор `Vector(unsigned int, int)` - версия с размер `size` и елемент `number`;
* Копиращ конструктор `Vector(const Vector&)`;
* Деструктор `~Vector()`;
* Оператор за присвояване `Vector& operator=(const Vector&)`;
* Селектор на полето за текущия размер на масива;
* Селектор на полето за текущия капацитет на масива;
* Метод, достъпващ началния елемент на динамичния масив `front` - константна и неконстантна версия;
* Метод, достъпващ последния елемент на динамичния масив `back` - константна и неконстантна версия;
* Метод, достъпващ елемент на динамичния масив `at` по `index`;
* Метод, който добавя елемент в края на динамичния масив `pushBack`;
* Метод, който премахва елемент от края на динамичния масив `popBack`;
* Метод, който премахва съдържанието на целия динамичен масив `clear`.
* Метод, който извежда съдържанието на масива на стандартния изход.

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Safety` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 6 ⌨️ (Дума) ⌨️

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/medium-yellow)

### 📖 Описание:

Създайте клас `Word`, представляващ контейнер за менажиране на символен низ. Контейнерът съдържа символен низ, заделен с точна големина, в който няма празни символи. Класът притежава следния интерфейс:

* Подразбиращ се конструктор `Word()`;
* Параметризиран конструктор `Word(const char*)`;
* Копиращ конструктор `Word(const Word&)`;
* Деструктор `~Word()`;
* Оператор за присвояване `Word& operator=(const Word&)`;
* Селектор за полето за символния низ.
* Метод, достъпващ началния елемент на символния низ `front`;
* Метод, достъпващ последния елемент на символния низ `back`;
* Метод, достъпващ елемент от символния низ `at` по `index`;
* Метод за сериализация на класа във файл;
* Метод за десериализация на класа от файл;
* Метод за конкатенация на две инстанции от класа;
* Метод, показващ дали инстанцията е празната дума.

създайте и външна функция, която конкатенира последователност от думи - масив, чиято максимална големина е `100` думи.

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Safety` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 7 🎓 (Университети) 🎓

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/medium-yellow)

### 📖 Описание:

Създайте програма за регистър на студенти и университети.

Създайте клас `Student`, представляващ студент. Студентът се характеризира с Факултетен Номер на студента спрямо конкретния университетски формат, първо име на студента, което е символен низ, заделен с точна големина, трето име на студента, което е символен низ заделен с точна големина, възраст на студента, която цяло положително число, среден успех на студента в университета, който е дробно число, специалност на студента в университета, която е изброима стойност. За студента реализирайте следната функционалност:

* Подразбиращ се конструктор `Student()`;
* Параметризиран конструктор `Student(const char*, const char*, const char*, unsigned int, double, const Specialty&)`;
* Копиращ конструктор `Student(const Student&)`;
* Деструктор `~Student()`;
* Оператор за присвояване `Student& operator=(const Student&)`;
* Селектори за всички полета - номера, имената, възрастта, средния успех и специалността;
* Мутатори само за полетата за възрастта, средния успех и специалността;

Класът да притежава и следните информационни методи:

* Метод, показващ дали студент има успех над `4.50`;
* Метод, показващ дали студент има успех под `4.50`;
* Метод, показващ дали студент е отличник или не.

Създайте клас `University`, представляващ университет, който има име, което е символен низ, заделен с точна големина, и трябва да съдържа максимален брой студенти предварително зададено число. Не може студент да съществува извън университета, защото в противен случай няма да е студент. Класът притежава следния интерфейс:

* Подразбиращ се конструктор `University()`;
* Параметризиран конструктор `University(const char*)`;
* Копиращ конструктор `University(const University&)`;
* Деструктор `~University()`;
* Оператор за присвояване `University& operator=(const University&)`, който е забранен;
* Метод за добавяне на студент в университета.
* Метод за премахване на студент от университета.
* Селектор за броя студенти в университета.
* Селектор за възможния капацитет студенти.
* Метод за намиране на студент по номера му.
* Метод за показване на броя отличници студенти.

Също така добавете и следната функционалност за университета:

* Създайте филтри за студентите по специалност в университет.
* Създайте филтри за студентите по диапазон на средния успех.
* Създайте метод, който да изчислява средния успех на всички студенти в университета.

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 8 📚 (Учебни Курсове) 📚

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/medium-yellow)

### 📖 Описание:

Създайте клас `Student`, представляващ студент. Студентът се описва с първо име и трето име, които са символни низове, заделени с точна големина, факултетен номер, който е уникален символен низ в рамките на програмата, оценки, които са не повече от `32` на брой. Класът да притежава следната функционалност:

* Подразбиращ се конструктор `Student()`
* Параметризиран конструктор `Student(const char*, const char*, const char*)`;
* Копиращ конструктор `Student(const Student&)`, който е забранен;
* Деструктор `~Student()`;
* Оператор за присвояване `Student& operator=(const Student&)`, който е забранен;
* Селектори за всички полета - имената и факултетния номер;
* Метод за добавяне на оценка;
* Метод за премахване на оценка;
* Метод за пресмятане на средния успех;
* Метод за сериализация на студент от поток;
* Метод за десериализация на студент от поток.

Създайте клас `Teacher`, представляващ учител. Учителят се описва с първо и трето име, които са символни низове, заделени с точна големина, както и име на катедра. Възможните катедри в рамките на нашата програма са `5`: `Algebra`, `Geometry`, `Computer Science`, `Probability`, `Calculus`. Класът притежава следния интерфейс:

* Подразбиращ се конструктор `Teacher()`, който е забранен;
* Параметризиран конструктор `Teacher(const char*, const char*, const Department&)`;
* Копиращ конструктор `Teacher(const Teacher&)`, който е забранен;
* Деструктор `~Teacher()`;
* Оператор за присвояване `Teacher& operator=(const Teacher&)`, който е забранен;
* Селектори за всички полета - имената и катедрата;
* Метод за сериализация на учител от поток;
* Метод за десериализация на учител от поток.

Създайте клас `Subject`, представляващ курс. Курсът се описва с име, студенти и се води от учител. Курсът може да добавя неограничено количество студенти. Не може да се добавят студенти към курс, към който няма назначен учител. Недопустимо е да се назначават повече от един учители в даден курс. Класът притежава следния интерфейс:

* Подразбиращ се конструктор `Subject()`, който е забранен;
* Параметризиран конструктор `Subject(const char*)`;
* Копиращ конструктор `Subject(const Subject&)`, който е забранен;
* Деструктор `~Subject()`;
* Оператор за присвояване `Subject& operator=(const Subject&)`, който е забранен;
* Метод за добавяне на студент в курса;
* Метод за назначаване на учител в курса;
* Селектори за всички полета - името и учителя;
* Метод за сериализация на предмет от поток;
* Метод за десериализация на предмет от поток.

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 9 🔠 (Стринг) 🔠

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/hard-red)

### 📖 Описание:

Създайте клас `String`, който представлява контейнер за символен низ, заделен с точна големина. Класът притежава следната функционалност::

* Подразбиращ се конструктор `String()`;
* Параметризиран конструктор `String(const char*)`;
* Копиращ конструктор `String(const String&)`;
* Деструктор `~String()`;
* Оператор за присвояване `String& operator=(const String&)`.

Класът притежава следните методи:

* Метод `size`, който връща размера на символния низ;
* Метод `length`, който връща размера на символния низ;
* Метод `capacity`, който връща количеството заделена памет за низа;
* Метод `clear`, който изчиства съдържанието на обекта, без да променя капацитета;
* Метод `empty`, който връща информация дали символният низ е празен;
* Метод `at`, който връща символ от низа на даден `index` - константна и неконстантна версия
* Метод `front`, който връща първия елемент на символния низ, ако има такъв - константна и неконстантна версия;
* Метод `back`, който връща последния елемент на символния низ, ако има такъв - константна и неконстантна версия;
* Метод `append`, който конкатенира нов символ, нов символен низ или нов стринг към вече съществуващ;
* Метод `pushBack`, който добавя нов елемент в края на символния низ;
* Метод `popBack`, който премахва елемент от края на символен низ;
* Метод `c_str`, който връща самия символен низ, който се съдържа в класа;
* Метод `data`, който връща самия символен низ, който се съдържа в класа;
* Метод `equals`, който проверява дали два низа са равни;
* Метод `find`, който намира индекса на първото срещане на даден символ `c`. Ако такъв символ не е намерен, да се върне резултат `-1`;
* Метод `find`, който намира индекса на първото срещане на даден подниз `s`. Ако такъв символ не е намерен, да се върне резултат `-1`;

Създайте клас `StringList`, който съдържа списък от подобни инстанции от направения клас `String`. Класът съдържа методи за сериализация и десериализация на съдържанието си, като използва конвенцията, че един символен низ винаги ще бъде на един ред. Класът притежава следния интерфейс:

* Подразбиращ се конструктор `StringList()`.
* Копиращ конструктор `StringList(const StringList&)`.
* Деструктор `~StringList()`.
* Оператор за присвояване `StringList& operator=(const StringList&)`.

* Метод `compareNumerical`, който проверява дали всички символни низове в колекцията са във възходящ ред спрямо числената наредба на дължината си.
* Метод `compareLexicogrp`, който проверява дали всички символни низове в колекцията са във възходящ ред спрямо лексикографската наредба на съдържанието си.

* Метод `filterKeyWord`, който извежда колкция само от онези символни низове, които имат конкретна ключова дума в себе си.
* Метод `transformWord`, който заменя всички срещания на думата `word1` с думата `word2`, в рамките на колекцията от низове.

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконстистентно или невалидно състояние. Напълно допустимо е добавите допълнителна функционалност, ако преценити за редно или необходимо. 

### 💡 Примери:


## Задача 10 🖼️ (Галерия) 🖼️

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/hard-red)

### 📖 Описание:

Създайте клас `Pixel`, представляващ пиксел. Пикселът се описва с трите стойности за червена наситеност, зелена наситеност, синя наситеност, съответно цели неотрицателни числа в интервала `[0 ; 255]`. Помислете дали има нужда да спазвате `Rule of 3` за този клас. Класът притежава следния интерфейс:

* Подразбиращ се конструктор `Pixel()`;
* Параметризиран конструктор `Pixel(unsigned int, unsigned int, unsigned int)`;
* Селектор за стойността на червена наситеност;
* Селектор за стойността на зелена наситеност;
* Селектор за стойността на синя наситеност.

Класът притежава следните информационни методи:

* Метод `isWhite`, който проверява дали пикселът е бял.
* Метод `isBlack`, който проверява дали пикселът е черен.
* Метод `isRed`, който проверява дали пикселът е червен.
* Метод `isGreen`, който проверява дали пикселът е зелен.
* Метод `isBlue`, който проверява дали пикселът е син.

Създайте клас `Image`, представляващ картина. Картината се описва чрез двумерен масив от пиксели, с подадени измерения на картината. Помислете как да създадете жизнения цикъл на обектите от класа `Image`. Класът притежава следния интерфейс:

* Подразбиращ се конструктор `Image()`, който е забранен;
* Параметризиран конструктор `Image(unsigned int, unsigned int)`;
* Копиращ конструктор `Image(const Image&)`, който е забранен;
* Деструктор `~Image()`;
* Оператор за присвояване `Image& operator=(const Image&)`;
* Селектори за измеренията на картината.

Класът притежава и следните методи за трансформация на картина.

* Метод `MedianFilter`, който осъществява `Median Filter` върху картината.
* Метод `LaplacianFilter`, който осъществява `Laplacian Filter` върху картината.
* Метод `Gaussian Filter`, който осъществява `Gaussian Filter` върху картината.
* Метод `NeighbourhoodAverageFilter`, който осъществява `Neighbourhood Average Filter` върху картината.
* Метод `BoxFilter`, който осъществява `BOX Filter` върху картината. 

Създайте клас `Gallery`, представляващ галерия. Галерията съдържа множество картини, както и капацитет. Към галерията може да се добавя картина до момента, в който общата площ на картините не надвиши капацитета на галерията. Съобразете, че при достигнат лимит на картините, е допустимо една картина да се размени с друга. Това не трябва да създава нови копия на картините, които ще се променят. Преценете как да направите така, че това число за всяка инстанция на класа на галерията да не се променя от външния свят. Класът притежава следния интерфейс:

* Пораметризиран конструктор `Gallery(unsigned int)`;
* Копиращ конструктор `Gallery(const Gallery&)`;
* Деструктор `~Gallery()`;
* Оператор за присвояване `Gallery& operator=(const Gallery&)`;
* Селектори за полетата на галерията;
* Метод `void add(const Image&)`, който добавя картина към галерията;
* Метод `void rem(const Image&)`, който премахва картина от галерията.

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 11 ⚙️ (Множество) ⚙️

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/hard-red)

### 📖 Описание:

Създайте клас `Set`, представляващ множество. Множеството съдържа числа без повторения в диапазона от `0` до `N - 1`, където `N` е естествено число в диапазона `[ 1 ; 256 ]`. Класът трябва да пази информация за съдържанието на числата в битове, т.е. ако съдържа дадено число, съответният бит ще бъде `1`, в противен случай `0`. Класът притежава следния интерфейс:

* Подразбиращ се конструктор `Set()`;
* Метод за добавяне на число `void add(int)`;
* Метод за премахване на число `void rem(int)`;
* Метод дали число се съдържа `bool ins(int)`;
* Извеждане на множеството в поток.

Създайте и следните методи в класа, като помислете има ли начин операциите зе имплементация на съответните методи да се реализират оптимално:

* Метод, който връща обединението на две множества.
* Метод, който връща сечението на две множества.
* Метод, който връща разликата на две множества.
* Метод, който връща симетричната разлика на две множества.
* Метод, който връща допълнението на множество.

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 12 🔮 (Магии) 🔮

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/hard-red)

### 📖 Описание:

Тео е на изпит по `Магия и Вълшебства`. Изпитът е теоретичен.

Създайте клас `Question`, представляващ въпрос от изпита. Въпросът има условие и точно четири отговора - символни низове, заделени с точна големина. Всеки от отговорите е верен или неверен - верният отговор е буква измежду `A, B, C, D`. Всеки въпрос има и точки, които носи - цяло неотрицателно число. Приемете, че всеки един въпрос има точно един верен отговор. Класът притежава следния интерфейс:

* Подразбиращ се конструктор `Question()`, който е забранен.
* Параметризиран конструктор `Question(const char*, const char**)`.
* Копиращ конструктор `Question(const Question&)`.
* Деструктор `~Question()`
* Оператор за присвояване `Question& operator=(const Question&)`;
* Селектори за всички полета - условието и отговорите;
* Метод за търсене на ключова дума в отговор на въпроса.

Създайте клас `Quiz`, представляващ изпит. Изпитът се състои от поредица от въпроси. Класът притежава следния интерфейс:

* Подразбиращ се конструктор `Quiz()`;
* Копиращ конструктор `Quiz(const Quiz&)`;
* Деструктор `~Quiz()`
* Оператор за присвояване `Quiz& operator=(const Quiz&)`;
* Метод за достъп до даден въпрос по номер на въпроса;
* Метод за добавяне на въпрос към изпита;
* Метод за премахване на въпрос от изпита;
* Метод за сериализация на изпита в поток;
* Метод за десериализация на изпита от поток.

Тео, както обикновено, не е учил за изпита, защото предната вечер е бил на участие на Селина. За щастие, той знае от свои приятели, че отговорите на въпросите винаги съдържат точно веднъж думата `Magic`, като тази дума винаги е трета в рамките на отговора на въпроса. Помогнете на Тео да си вземе изпита, като напишете програма, която прочита данните за поредица от въпроси от текстов файл с избран по ваш избор формат на данните, и пресмята дали Тео ще си вземе изпита. За да се случи това, от отделен файл се четат и неговите отговори, които той е посочил на всеки един въпрос. Тео си е взел изпита, ако има поне половината от точките за целия изпит.

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 13 🌲 (Гори) 🌲

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/impossible-black)

### 📖 Описание:

`Диференциалчо` иска да намери `Интегралка`, защото `Интегралка` се е загубила в древните гори.

Създайте клас `Forest`, представляващ гора. Гората се описва чрез поредица от дървета и чрез изречение, което е символен низ, заделен с точна големина, поредица от числа, както и идентификатор цяло число, който не се променя никога за гората. Гората никога не се променя и нейните данни остават непроменими за коя да е инстанция на класа. Класът притежава следния интерфейс:

* Подразбиращ се конструктор `Forest()`, който е забранен;
* Параметризиран конструктор `Forest(const char*, const int*, int)`;
* Копиращ конструктор `Forest(const Forest&)`, който е забранен;
* Деструктор `~Forest()`;
* Оператор за присвояване `Forest& operator=(const Forest&)`, който е забранен;
* Селектори за всички полета - изречението и идентификатора.

Създайте клас `Tree`, представляващо дърво. Дървото се описва чрез дума, която е символен низ, заделен с точна големина, и цяло неотрицателно число. Класът притежава следния интерфейс:

* Подразбиращ се конструктор `Tree()`, който е забранен;
* Параметризиран конструктор `Tree(const char*, int)`;
* Копиращ конструктор `Tree(const Tree&)`, който е забранен;
* Деструктор `~Tree()`;
* Оператор за присвояване `Tree& operator=(const Tree&)`, който е забранен;
* Нотификация за принадлежност или непринадлежност към дадена гора;
* Селектори за всички полета - думата и идентификатора.

Гората, в която се намира `Интегралка`, е именно онази гора, която се състои от онези дървета, чийто думи образуват изречението на гората и чийто числа образуват поредицата на гората. Помогнете на `Диференциалчо`, като създадете програма, която чете от стандартния вход данните за горите и техните дървета и извежда на стандартния изход идентификатора на гората, в която се намира `Интегралка`. При невалидни данни или невъзможност за заделяне на памет да се хвърля изключение.

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:



</div>
