# Седмица 8
# Предефиниране на оператори - основни концепции. Бинарни оператори.



<div align="justify">



## Задача 1 📅 (Дати) 📅

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/easy-green)

### 📖 Описание:

Създайте клас `Date`, представляващ дата от календата. Датата от календара пази в себе си информация за следните полета:

* Ден - поле от тип `std::uint8_t`;
* Месец - поле от тип `std::uint8_t`;
* Година - поле от тип `std::uint16_t`.

Приемаме, че валидните години са `UNIX` базирани, т.е. не може да съществува дата преди `01.01.1970`. Класът не трябва да притежава конструктор по подразбиране. Класът притежава следния интерфейс:

* Параметризиран конструктор `Date(std::uint8_t, std::uint8_t, std::uint8_t)`;
* Селектори за всички полета - за деня, месеца и годината;
* Метод за извеждане на данните в изходен поток.

Класът ви трябва да има следните предефинирани методи:

* Оператор `==` за сравнение дали две дати са еднакви;
* Оператор `!=` за сравнение дали две дати не са еднакви.

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 2 🧺 (Пинокио) 🧺

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/easy-green)

### 📖 Описание:

Майстор Череша е изпратил Пинокио да събере ябълки от специално дърво в гората. За целта той дал на Пинокио кошница. Но, както сами се досещате, това не била каква да е кошница, а магическа. В какво се състояла магията на кошницата, дадена на Пинокио ли? Тя могла да побира произволно количество ябълки. Ябълките представляват символни низове, заделени с точна големина, както и самата дължина. Тази кошница, която вие трябва да направите, е такава, че, ако в даден момент в кошницата на Пинокио има `N` ябълки, то тя може да има свободно място за най-много още `N` ябълки. Кошницата на Пинокио трябва да позволява добавянето на нова ябълка и премахването на ябълка по даден низ, който я описва. Помислете какъв интерфейс да използвате и как да го имплементирате. Реализирайте следните оператори за кошницата на Пинокио:

* Оператор `[]`, който по зададен индекс, ако той е валиден, връща ябълката, което се намира там;
* Оператор `[]`, който по зададен символен низ, връща първата ябълка, която я съдържа, ако има такова;
* Оператор `+`, който конкатенира `2` различни кошници и резултатът е кошница;
* Оператор `+`, който конкатенира низ с всички ябълки в дадена кошница и резултатът е кошница;
* Оператор `+=`, който конкатенира `2` различни кошници и резултатът е кошница;
* Оператор `+=`, който конкатенира низ с всички ябълки в дадена кошница и резултатът е кошница.

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 3 ⚡ (Динамичен Масив) ⚡

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/easy-green)

### 📖 Описание:

Създайте клас `Vector`, който представлява `N`-мерен вектор, като `N` може да бъде произволно голямо число (`unsigned int`). Всеки от компонентите на вектора е от тип дробно число - `double`. Съобразете какъв интерфейс трябва да притежава вектора. Имплементирайте следните оператори:

* Оператор `[]`, който приема аргумент за индекс, и връща съответният компонент на този индекс, при достъпване на грешен компонент се връща `0`;
* Оператор `+`, който извършва събиране на вектори с еднаква размерност, иначе отказва операцията;
* Оператор `-`, който извършва изваждане на вектори с еднаква размерност, иначе отказва операцията;
* Оператор `*`, който извършва умножение на вектор с число (скалиране);
* Оператор `/`, който извършва деление на вектор с число (скалиране);
* Оператор `+=`, който извършва събиране на вектори с еднаква размерност, иначе отказва операцията;
* Оператор `-=`, който извършва изваждане на вектори с еднаква размерност, иначе отказва операцията;
* Оператор `*=`, който извършва умножение на вектор с число (скалиране);
* Оператор `/=`, който извършва деление на вектор с число (скалиране);
* Оператор `<`, който извършва съответната лексикографска наредба над елементите на два вектора;
* Оператор `>`, който извършва съответната лексикографска наредба над елементите на два вектора;
* Оператор `<=`, който извървша съответната лексикографска наредба над елементите на два вектора;
* Оператор `>=`, който извършва съответната лексикографска наредба над елементите на два вектора;
* Оператор `()`, който приема аргумент едноместна функция, която се прилага над всеки един аргумент на масива.

За оператора `()` помислете дали може да се използва и друг аргумент, освен указател към функция...

Демонстрирайте работата вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимио от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 4 🔢 (Обикновени Дроби) 🔢

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/easy-green)

### 📖 Описание:

Създайте клас `Rational`, представляващ обикновена дроб. Обикновената дроб, както знаем, има числител и знаменател - това са цели числа. Имплементирайте необходимите оператори за класа:

* Оператор `+`, който събира две обикновени дроби;
* Оператор `-`, който изважда две обикновени дроби;
* Оператор `*`, който умножава две обикновени дроби;
* Оператор `/`, който дели две обикновени дроби;
* Оператор `==` сравнява две обикновени дроби са еднакви;
* Оператор `!=` сравнява две обикновени дроби са различни;
* Оператор `<`, който имплементира поведението на релацията "по-малко";
* Оператор `>`, който имплементира поведението на релацията "по-голямо";
* Оператор `<=`, който имплементира поведението на релацията "по-малко или равно";
* Оператор `>=`, който имплементира поведението на релацията "по-голямо или равно";
* Оператор `+=` събира една обикновена дроб към друга;
* Оператор `-=` събира една обикновена дроб към друга;
* Оператор `*=` събира една обикновена дроб към друга;
* Оператор `/=` събира една обикновена дроб към друга;
* Оператор `>>` за четене на обикновена дроб от входен поток;
* Оператор `<<` за извеждане на обикновена дроб на стандартния изход;

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 5 💰 (Парички) 💰

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/medium-yellow)

### 📖 Описание:

Да се реализира изброим тип `Currency`, който да пази следните стойности: `BGN`, `USD`, `EUR`. Напишете структура `ConversionRate`, която ще съдържа следната информация: обменен курс, стара валута, нова валута. Валутите са от тип `Currency`.

Създайте клас `Money`, представляващ пари, който да пази в себе си следната информация: сума пари, които са реално число, и вид валута. Класът притежава следния интерфейс:

* Подразбиращ се конструктор `Money()`, който е забранен;
* Параметризиран конструктор `Money(double, const Currency&)`;
* Селектори за полетата на класа - сумата пари и валутата;
* Мутаторите за полетата на класа не трябва да са позволени;
* Метод `softConvert(const ConversionRate& convertion) const`, който не променя стойностите на класа, но променя копие на класа и връща него;
* Метод `hardConvert(const ConversionRate& newCurrency)`, който променя стойностите на самия клас.

Класът трябва да предефиниран следните оператори, като, където е допустимо, нека са комутативни:

* Оператори за събиране и изваждане с реални числа - `+`, `-`, `+=`, `-=`;
* Оператори за събиране и изваждане с цели числа - `+`, `-`, `+=`, `-=`;
* Оператори за събиране и изваждане с обекти от тип `Money` - `+`, `-`, `+=`, `-=`;
* Оператор унарен `-`, така че, като е поставен пред обекта, да обръща неговата стойност;
* Оператори за наредба - `<`, `>`, `<=`, `>=`;
* Оператори за работа с потоци - `<<` и `>>`;
* Оператори за преобразуване към `int` и `double`.

Създайте клас `Client`, представляващ клиент, който да пази в себе си следната информация: име, което е символен низ с точна големина, ЕГН, което има точен формат, рожден ден, пари, които са обект от тип `Money`. Класът притежава следния интерфейс:

* Параметризиран конструктор `Client(const char*, const char*, const Date&, const Money&)`;
* Селектори за всички полета на класа - името, ЕГН, рождения ден, парите;
* Мутатори за всички полета на класа - името, ЕГН, рождения ден, парите.

Създайте клас `Bank`, представляащ банка, който да пази в себе си следната информация: име на банката, която е символен низ, заделен с точна големина, потребители, които са саморазширяващ се масив от `Client`, големина на потребителите, капацитет на потребителите, обменни курсове, които са масив от точно `6` обекта от тип `ConversionRate`. Трябва да има обменен курс от всяка валута към всяка друга. Класът да има следния интерфейс:

* Подразбиращ се конструктор `Bank()`, който е забранен;
* Параметризиран конструктор `Bank(const char*)`;
* Метод `addClient`, който добавя клиент към банката;
* Метод `remClient`, който премахва клиент от банката;
* Метод `getClientIndex`, който достъпва потребител от банката;
* Метод `deposit`, който депозира пари в банката на един клиент;
* Метод `withdraw`, който изтегля пари от банката за един клиент;
* Метод `void transfer(Client& source, Client& destination)`, който трябва да провери дали съществуват клиентите, после дали `source` клиентът има достатъчно пари да прехвърли към `destination` клиента, а накрая маха парите от `source` и ги дава на `destination`. При различни валути може да се наложи конвертиране на парите;
* Метод `convertClientMoney`, който конвертира парите на клиент от банката.

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възнкиване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 6 🛒 (Сортиран Масив) 🛒

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/medium-yellow)

### 📖 Описание:

Създайте клас `SortedArray`, който представлява контейнер за винаги сортирани цели числа, чийто размер може да е произволен. Предефинирайте съответните методи на класа:

* Оператор `+`, който добавя число към всеки елемент на контейнера.
* Оператор `-`, който изважда число от всеки елемент на контейнера.
* Оператор `*`, който умножава число с всеки елемент на контейнера.
* Оператор `/`, който дели всеки елемент на контейнера на числото.
* Оператор `()`, който приема едноместна функция, която прилага над всеки елемент на масива.
* Оператор `<`, който прилага съответната наредба между два контейнера.
* Оператор `>`, който прилага съответната наредба между два контейнера.
* Оператор `<=`, който прилага съответната наредба между два контейнера.
* Оператор `>=`, който прилага съответната наредба между два контейнера.

Демонстрирайта работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконстистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 7 🧵 (Стринг) 🧵

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/medium-yellow)

### 📖 Описание:

Създайте клас `String`, който представлява контейнер за символен низ. Символният низ в контейнера е заделен с точна големина. Съобразете има ли начин да избегнете честите копирания при работата с обекти от класа. Класът притежава следния интерфейс:

* Подразбиращ се конструктор `String()`;
* Параметризиран конструктор `String(const char*)`;
* Копиращ конструктор `String(const String&)`;
* Преместващ конструктор `Strong(String&&)`;
* Деструктор `~String()`;
* Оператор за присвояване `String& operator=(const char*)`;
* Оператор за присвояване `String& operator=(const String&)`;
* Оператор за преместване `String& operator=(String&&)`;
* Селектор за размера на символния низ;
* Селектор за дължината на символния низ;
* Селектор за капацитета на символния низ;
* Селектор за максималния размер на символния низ;
* Селектор за полето за самия символен низ;
* Метод `empty()`, проверяващ дали символния низ е празен;
* Метод `front()`, който връща началния елемент на низа - константна версия;
* Метод `front()`, който връща началния елемент на низа - неконстнатна версия;
* Метод `back()`, който връща последния елемент на низа - константна версия;
* Метод `back()`, който връща последния елемент на низа - неконстантна вресия;
* Метод `int compare(const char*)`;
* Метод `int compare(const String&)`;
* Метод `at()`, достъпващ елемент на символния низ по индекс - константна версия;
* Метод `at()`, достъпващ елемент на символния низ по индекс - неконстантна версия;

Класът да предефинира следните оператори:

* Оператор `[]`, достъпващ елемент на символния низ по индекс - константна версия;
* Оператор `[]`, достъпващ елемент на символния низ по индекс - неконстантна версия;
* Оператор `==`, сравняващ с инстанция на класа или с друг символен низ;
* Оператор `!=`, сравняващ с инстанция на класа или с друг символен низ;
* Оператор `<`, прилагащ наредбата върху инстанция на класа или друг символен низ;
* Оператор `>`, прилагащ наредбата върху инстанция на класа или друг символен низ;
* Оператор `<=`, прилагащ наредбата върху инстанция на класа или друг символен низ;
* Оператор `>=`, прилагащ наредбата върху инстанция на класа или друг символен низ;

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 8 ()

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/medium-yellow)

### 📖 Описание:

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 9 ()

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/hard-red)

### 📖 Описание:

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невзъможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 10 ()

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/hard-red)

### 📖 Описание:

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 11 ()

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/hard-red)

### 📖 Описание:

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невзъможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 12 ()

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/hard-red)

### 📖 Описание:

Създайте клас `Vertex`, представляващ връх в граф. Върхът в граф се описав с етикет, който е символен низ, заделен с точна големина. Класът притежава следния интерфейс:

* Подразбиращ се конструктор `Vertex()`, който е забранен;
* Параметризиран конструктор `Vertex(const char*)`;
* Копиращ конструктор `Vertex(const Vertex&)`;
* Деструктор `~Vertex()`;
* Оператор за присвояване `Vertex& operator=(const Vertex&)`;
* Селектор за етикета на върха.

Създайте клас `Egde`, представляващ ребро. Ребрата се описват чрез двойка от върхове и тегло, което е реална стойност. Класът притежава следния интерфейс:

* Подразбиращ се конструктор `Edge()`, който е забранен;
* Параметризиран конструктор `Edge(const Vertex&, const Vertex&, double)`;
* Селектор за всички полета на класа - върховете и теглото;

Създайте клас `Graph`, представляващ граф. Приемаме, че в графът всички ребра са ненасочени. Графът се описва чрез списък от ребра:

* Подразбиращ се конструктор `Graph()`;
* Копиращ конструктор `Graph(const Graph&)`;
* Деструктор `~Graph()`;
* Оператор за присвояване `Graph& operator=(const Graph&)`;

Класът за граф притежава и следните алгоритмични методи:

* Метод за прилагане на алгоритъма `Breath First Search` по даден връх в графа;
* Метод за прилагане на алгоритъма `Depth First Search` по даден връх в графа;
* Метод за намиране на най-кратък път в грфа от един връх до друг връх.

За класа предефинирайте следните оператори върху граф и ребро:

* Оператор `+`, който приема ребро и добавя ребро към графа;
* Оператор `-`, който приема ребро и премахва ребро от графа;
* Оператор `+=`, който приема ребро и добавя ребро към графа;
* Оператор `-=`, който приема ребро и премахва ребро от графа.

За класа предефинирайте и следните оператори като външни за класа:

* Оператор `<`, който проверява съответната релация върху сумите от теглата на два графа;
* Оператор `>`, който проверява съответната релация върху сумите от теглата на два графа;
* Оператор `<=`, който проверява съответната релация върху сумите от теглата на два графа;
* Оператор `>=`, който проверява съответната релация върху сумите от теглата на два графа.

За класа предефинирайте и следните оператори за изоморфност на два графа:

* Оператор `==`, който проверява дали два графа са изоморфни;
* Оператор `!=`, който проверява дали два графа не са изоморфни.

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създавате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:


## Задача 13 ()

### 🧠 Сложност: &nbsp; ![Static Badge](https://img.shields.io/badge/impossible-black)

### 📖 Описание:

Демонстрирайте работата на вашата програма. Съблюдавайте добрите ООП практики. При некоректни данни или невъзможност за заделяне на памет да се хвърля изключение. Грижете се за коректно използване на паметта и всички системни ресурси. Трябва да поддържате `Strong Exception Guarantee` за вашите класове, които създаате в програмата - не трябва да допускате изтичане на памет, независимо от възникване на някакво изключение, както и получаване на обекти в неконсистентно или невалидно състояние. Напълно допустимо е да добавите допълнителна функционалност, ако прецените за редно или необходимо.

### 💡 Примери:



</div>
